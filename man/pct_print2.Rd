% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pct_print.R
\name{pct_print2}
\alias{pct_print2}
\title{Format a pre-computed proportion or percentage}
\usage{
pct_print2(
  pct,
  digits = 2,
  conv = 100,
  prefix = "",
  suffix = "\%",
  na_token = "--",
  ...
)
}
\arguments{
\item{pct}{Numeric vector. Value(s) to format.}

\item{digits}{Integer. Number of decimal places. Default \code{2}.}

\item{conv}{Numeric. Multiplier applied before formatting. Use \code{100} when
\code{pct} is a proportion (0--1); use \code{1} when it is already a percentage
(0--100). Default \code{100}.}

\item{prefix}{Character. String prepended to each result. Default \code{""}.}

\item{suffix}{Character. String appended to each result. Default \code{"\%"}.}

\item{na_token}{Character. Replacement string for \code{NA} values.
Default \code{"--"}.}

\item{...}{Ignored; retained for future extensibility.}
}
\value{
A character vector of formatted percentages.
}
\description{
Applies an optional multiplier (\code{conv}), rounds to \code{digits} decimal places,
and returns a formatted character string. \code{NA} values are replaced with
\code{na_token} before any string formatting is applied.
}
\examples{
pct_print2(c(0.75, NA, 0.5))         # "75.00\%"  "--\%" "50.00\%"
pct_print2(75, conv = 1, digits = 0) # "75\%"

}
