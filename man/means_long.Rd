% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom_mean.R
\name{means_long}
\alias{means_long}
\title{Flexible mean calculator for use inside dplyr summarise() pipelines}
\usage{
means_long(
  values,
  group,
  is_complete,
  id_col,
  is_geometric = TRUE,
  is_paired = FALSE
)
}
\arguments{
\item{values}{Numeric vector of values.}

\item{group}{Character. Name of the column in the internal working frame.}

\item{is_complete}{Logical vector. \code{TRUE} for subjects present at all
timepoints (same length as \code{values}).}

\item{id_col}{Vector. Subject identifiers (same length as \code{values}).}

\item{is_geometric}{Logical. If \code{TRUE} (default), returns geometric mean;
\code{FALSE} returns arithmetic mean.}

\item{is_paired}{Logical. If \code{TRUE}, restricts computation to subjects
where \code{is_complete == TRUE}. Default \code{FALSE}.}
}
\value{
A single numeric value, or \code{NA} if the geometric mean cannot be
computed (non-positive values present).
}
\description{
Computes a geometric or arithmetic mean over \code{values}, optionally restricted
to subjects with complete data at all timepoints (\code{is_paired = TRUE}).
}
\examples{
means_long(c(1, 4, 16, 8), "conc",
           is_complete = c(TRUE, TRUE, FALSE, TRUE),
           id_col = 1:4)

}
