% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/save_units_in_df.R
\name{apply_units_from_df}
\alias{apply_units_from_df}
\title{Restore unit strings to a data frame}
\usage{
apply_units_from_df(df, units_spec)
}
\arguments{
\item{df}{A data frame whose unit columns need to be restored.}

\item{units_spec}{A data frame as returned by \code{\link[=save_units_in_df]{save_units_in_df()}}, with
columns \code{column_name} and \code{unit}.}
}
\value{
\code{df} with the specified columns converted to \code{units}-class objects.
}
\description{
Re-attaches units to columns of \code{df} using a spec produced by
\code{\link[=save_units_in_df]{save_units_in_df()}}. Columns absent from \code{df} are silently skipped,
making it safe to call on a data frame that has been reshaped since the
snapshot was taken.
}
\examples{
\dontrun{
library(units)
df <- data.frame(conc = c(10, 20), time = c(0, 1))
spec <- data.frame(column_name = c("conc", "time"),
                   unit        = c("ng/mL", "h"))
df_restored <- apply_units_from_df(df, spec)
}

}
