% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom_mean.R
\name{geom_mean_paired}
\alias{geom_mean_paired}
\title{Geometric mean restricted to complete pairs}
\usage{
geom_mean_paired(x, y)
}
\arguments{
\item{x}{Numeric vector (values to average).}

\item{y}{Numeric vector (used only to determine complete cases).}
}
\value{
A single numeric value, or \code{NA} if any paired \code{x} value is \verb{<= 0}.
}
\description{
Computes the geometric mean of \code{x} using only subjects that have non-\code{NA}
values in \strong{both} \code{x} and \code{y}. \code{y} is used solely to identify complete
cases; the mean itself is over \code{x}.
}
\details{
Useful for pre/post comparisons where the baseline GM should be computed
on the same subjects that have a post-treatment value.
}
\examples{
geom_mean_paired(c(1, 2, NA), c(10, NA, 30))  # GM of c(1) — only pair 1 is complete

}
