% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coalesce_after_join.R
\name{merge_suffix_columns}
\alias{merge_suffix_columns}
\title{Resolve duplicate columns after a join}
\usage{
merge_suffix_columns(df)
}
\arguments{
\item{df}{A data frame, typically the result of \code{\link[dplyr:mutate-joins]{dplyr::left_join()}} or a
similar join that produces \code{.x} / \code{.y} suffix columns.}
}
\value{
A data frame with \code{.x} / \code{.y} column pairs replaced by single
coalesced columns carrying the base name.
}
\description{
After a \code{dplyr} join produces paired \code{.x} / \code{.y} columns, this function
coalesces each pair into a single column: the \code{.x} value is used when
non-\code{NA}, otherwise the \code{.y} value is used. The original \code{.x} / \code{.y}
columns are then dropped.
}
\details{
Only column pairs where \strong{both} the \code{.x} and \code{.y} variants are present are
processed. Unpaired \code{.x}-only or \code{.y}-only columns are left untouched.
}
\examples{
library(dplyr)
df_a <- data.frame(id = 1:3, val = c(1, NA, 3))
df_b <- data.frame(id = 1:3, val = c(10, 20, 30))
joined <- left_join(df_a, df_b, by = "id")
merge_suffix_columns(joined)

}
