% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/traverse.R
\name{get_list_names}
\alias{get_list_names}
\title{Enumerate all element paths in a nested list}
\usage{
get_list_names(x, parent = "")
}
\arguments{
\item{x}{A list to traverse.}

\item{parent}{Character. Prefix prepended to each path. Used internally
during recursion; leave at the default \code{""} when calling directly.}
}
\value{
A character vector of element paths. Returns \code{character(0)} if \code{x}
is not a list.
}
\description{
Recursively traverses a nested list and returns the access path to every
element. Named elements are represented as \code{parent$name}; unnamed elements
as \code{parent[[i]]}.
}
\examples{
lst <- list(a = 1, b = list(c = 2, d = list(e = 3)))
get_list_names(lst)
# [1] "a"      "b"      "b$c"    "b$d"    "b$d$e"

# Unnamed elements use [[i]] notation
get_list_names(list(1, list(2, 3)))
# [1] "[[1]]"       "[[2]]"       "[[2]][[1]]"  "[[2]][[2]]"

}
