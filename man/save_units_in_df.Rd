% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/save_units_in_df.R
\name{save_units_in_df}
\alias{save_units_in_df}
\title{Snapshot unit strings from a data frame}
\usage{
save_units_in_df(df)
}
\arguments{
\item{df}{A data frame.}
}
\value{
A data frame with columns:
\describe{
\item{\code{column_name}}{Character. Name of the column that carries units.}
\item{\code{unit}}{Character. Unit string (as returned by \code{\link[units:units]{units::units()}}).}
}
Only columns that actually carry units are included; non-unit columns are
excluded so the spec contains only actionable entries for
\code{\link[=apply_units_from_df]{apply_units_from_df()}}.
}
\description{
Records the unit of every \code{units}-class column in \code{df} and returns them
as a two-column data frame. Use this before operations that strip units
(e.g., \code{\link[tidyr:pivot_wider]{tidyr::pivot_wider()}}, \code{\link[dplyr:bind_rows]{dplyr::bind_rows()}}), then restore with
\code{\link[=apply_units_from_df]{apply_units_from_df()}}.
}
\examples{
\dontrun{
library(units)
df <- data.frame(
  conc = set_units(c(10, 20), "ng/mL"),
  time = set_units(c(0, 1), "h")
)
spec <- save_units_in_df(df)
# spec has 2 rows: conc -> "ng/mL", time -> "h"
}

}
